---
interface Props {
  sectionName: string;
}

const { sectionName } = Astro.props;
---

<div class="flex pb-4 md:pb-8 lg:pb-10 w-full">
  <h2 class="text-3xl sm:text-4xl md:text-4xl font-bold" aria-label={sectionName}>{`<${sectionName} />`}</h2>
</div>
